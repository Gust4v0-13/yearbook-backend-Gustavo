info:
  name: Criar desenho sem token
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/desenhos"
  body:
    type: json
    data: |-
      {
        "titulo": "Desenho sem token teste"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar 401", function() {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5